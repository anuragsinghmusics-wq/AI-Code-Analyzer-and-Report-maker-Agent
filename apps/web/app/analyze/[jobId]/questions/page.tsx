// apps/web/app/analyze/[jobId]/questions/page.tsx — Questions view placeholder
export default function QuestionsPage({ params }: { params: { jobId: string } }) {
  return <div>Questions for job: {params.jobId}</div>;
}
