FROM node:20-alpine AS base

WORKDIR /app

# TODO: Implement multi-stage Docker build for worker
# Stage 1: Install dependencies
# Stage 2: Build TypeScript
# Stage 3: Production runtime

CMD ["node", "dist/index.js"]
